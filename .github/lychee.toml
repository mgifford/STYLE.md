include_mail = false
accept = [200, 206, 429]
max_concurrency = 12

# Common false positives in CI and local placeholder links.
exclude = [
  "^http://localhost",
  "^http://127\\.0\\.0\\.1",
  "^https://example\\.com",
  "^https://github\\.com"
]
